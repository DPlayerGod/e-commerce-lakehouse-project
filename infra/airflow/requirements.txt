# Airflow additional requirements
# Base image apache/airflow:3.1.6 already includes standard providers
# Add any additional packages here if needed

apache-airflow-providers-apache-spark==4.11.3

# Pin PySpark to match Spark 3.5.x cluster (avoids downloading 455MB PySpark 4.x)
pyspark==3.5.6

# Python dependencies needed by Spark jobs running via spark-submit
# (driver runs on Airflow container in client mode)
boto3>=1.34.0
pyarrow>=14.0.0
